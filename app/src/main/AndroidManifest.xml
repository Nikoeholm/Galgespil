<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nikolaj.galgespil">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Velkomst_akt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Spil_akt" android:configChanges="orientation|screenSize"/>
        <activity android:name=".Regler_akt" />
        <activity android:name=".Indstillinger_akt" />
        <activity android:name=".ListView_akt" />
        <activity android:name=".Hichscore_akt"></activity>
    </application>

</manifest>